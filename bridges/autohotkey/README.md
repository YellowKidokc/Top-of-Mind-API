# AutoHotkey Bridge

This bridge is the first Windows "hands" layer.

Responsibilities:

- hotkeys
- clipboard capture
- type text into local AI/app windows
- click send/start/stop buttons
- follow calibrated input boxes
- optionally trigger OCR for a small window rectangle
- post results back to the API

The tested example script currently lives in `apps/api/scripts/autohotkey/`.
